

"""
#referenced https://cs.stackexchange.com/questions/12853/heap-give-an-on-lg-k-time-algorithm-to-merge-k-sorted-lists-into-one-so
if listA = [k][?]: #gets the input of the lists
    c = 0 #index of the results
    result[n] #the output
    heap[k] #stores the index into the list then compares the value


#to populate the heap
for i = 1:k #runs O(k) times
    heap.insert(0,k) #O(log k)

#deletes the minimum, inserts to the next value of the list into the heap
while !heap.empty(): #runs the o(n) times
    if i, k = heal.deleteMin(): #O(log k)
        result = c += 1 
    i += 1
    if i <= len.listA[k]: #insert only if not end-of-list
        heap.insert(i, k) #o(log k)
"""

#referenced #referenced https://www.geeksforgeeks.org/heap-sort-for-decreasing-order-using-min-heap/
#o(n log k)
def heapify(a, b, c):
    variables
    if "case 1":
        "case 1"

    if "2":
        "case 2"
    
    if"3":
        "case 3"

        heapify(a, b, c)
def heapSort(a, b):
    if "case 4":
        "case 4"
    
    for ("case 5"):
        "case 5"
    
    heapify(arr, i, 0)
    
#main
length = int(len(arr)) #to gget the length
other variables to radix + bucket sort
emptylist = []

for "case 5"(0,100):
    "case 6"


Heapsorted(list, arr)



